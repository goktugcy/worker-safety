# Example workflow for a project that uses Worker Safety.
# Copy this into your own repository as .github/workflows/worker-safety.yml.
#
# It lives outside .github/workflows on purpose: it runs `vendor/bin/worker-safety`,
# which only exists once the package is installed as a dependency, so it is a
# template rather than a workflow for this repository.
name: Worker Safety

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  worker-safety:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: shivammathur/setup-php@v2
        with:
          php-version: '8.3'
          coverage: none

      - run: composer install --no-interaction --no-progress

      - run: vendor/bin/worker-safety scan --fail-on=high --no-ansi
